import * as React from 'react';
import SvgIcon, { ISvgIconProps } from './util';

export default function TurnTop(props: ISvgIconProps) {
  return (
    <SvgIcon {...props}>
      <path d="M256.32 640l256-256 256 256z"></path>
    </SvgIcon>
  );
}
